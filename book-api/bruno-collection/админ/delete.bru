meta {
  name: Delete User
  type: http
  seq: 15
}

http {
  method: DELETE
  url: {{baseUrl}}/api/admin/users/3
  headers {
    Authorization: Bearer {{adminToken}}
  }
}

tests {
  console.log("User deletion response:", response.body);
}