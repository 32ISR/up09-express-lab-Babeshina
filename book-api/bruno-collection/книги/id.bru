meta {
  name: Get Book by ID
  type: http
  seq: 7
}

http {
  method: GET
  url: {{baseUrl}}/api/books/1
}

tests {
  const book = response.body;
  if (book.id) {
    console.log("Book found:", book.title);
  }
}