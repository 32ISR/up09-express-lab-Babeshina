meta {
  name: Get Reviews
  type: http
  seq: 12
}

http {
  method: GET
  url: {{baseUrl}}/api/books/1/reviews
}

tests {
  const reviews = response.body;
  if (Array.isArray(reviews)) {
    console.log(`Found ${reviews.length} reviews for book 1`);
  }
}