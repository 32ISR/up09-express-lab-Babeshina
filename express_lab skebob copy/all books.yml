info:
  name: all books
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}api/books?genre=Драма"
  params:
    - name: genre
      value: Драма
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
