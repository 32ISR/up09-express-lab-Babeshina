info:
  name: all reviews
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}api/books/4/reviews"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
