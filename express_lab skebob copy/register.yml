info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}api/auth/register"
  body:
    type: json
    data: |-
      {
            "username": "skebob234",
            "email": "nskebob@gmail.com",
            "password": "skebob123555"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
